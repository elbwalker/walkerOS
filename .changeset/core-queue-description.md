---
'@walkeros/core': patch
---

The destination `queue` option's description now says what it does: whether a
destination added later receives the run's earlier events (default true).
Consent-denied events are queued regardless.
