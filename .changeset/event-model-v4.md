---
'@walkeros/core': major
'@walkeros/collector': major
'@walkeros/cli': major
'@walkeros/mcp': major
'@walkeros/web-source-browser': major
'@walkeros/web-source-datalayer': major
'@walkeros/web-source-cmp-cookiefirst': major
'@walkeros/web-source-cmp-cookiepro': major
'@walkeros/web-source-cmp-usercentrics': major
'@walkeros/web-source-session': major
'@walkeros/source-demo': major
'@walkeros/server-source-express': major
'@walkeros/server-source-fetch': major
'@walkeros/server-source-gcp': major
'@walkeros/server-source-aws': major
'@walkeros/server-destination-pinterest': major
'@walkeros/server-destination-bing': major
'@walkeros/server-destination-meta': major
'@walkeros/server-destination-tiktok': major
'@walkeros/server-destination-snapchat': major
'@walkeros/server-destination-criteo': major
'@walkeros/server-destination-sqlite': major
'@walkeros/server-destination-file': major
'@walkeros/server-destination-amplitude': major
'@walkeros/server-destination-customerio': major
'@walkeros/server-destination-hubspot': major
'@walkeros/server-destination-kafka': major
'@walkeros/server-destination-klaviyo': major
'@walkeros/server-destination-mixpanel': major
'@walkeros/server-destination-posthog': major
'@walkeros/server-destination-redis': major
'@walkeros/server-destination-rudderstack': major
'@walkeros/server-destination-segment': major
'@walkeros/server-destination-twitter': major
'@walkeros/server-destination-datamanager': major
'@walkeros/server-destination-mparticle': major
'@walkeros/server-destination-linkedin': major
'@walkeros/server-destination-reddit': major
'@walkeros/server-destination-api': major
'@walkeros/server-destination-slack': major
'@walkeros/server-destination-gcp': major
'@walkeros/server-destination-aws': major
'@walkeros/web-destination-gtag': major
'@walkeros/web-destination-plausible': major
'@walkeros/web-destination-piwikpro': major
'@walkeros/web-destination-optimizely': major
'@walkeros/web-destination-segment': major
'@walkeros/web-destination-meta': major
'@walkeros/web-destination-amplitude': major
'@walkeros/web-destination-clarity': major
'@walkeros/web-destination-mixpanel': major
'@walkeros/web-destination-fullstory': major
'@walkeros/web-destination-snowplow': major
'@walkeros/web-destination-heap': major
'@walkeros/web-destination-pinterest': major
'@walkeros/destination-demo': major
'@walkeros/transformer-validator': major
'@walkeros/web-core': major
---

Event model v4: breaking changes to the `Event`, `Source`, and `Entity` shapes.

- `event.id` is now a W3C span_id (16 lowercase hex chars), generated by the
  collector. Reference: W3C Trace Context (W3C Recommendation, January 2020).
- `event.version`, `event.group`, `event.count` are removed.
- `source.type` is now the source kind (e.g. `browser`, `gtag`, `mcp`, `cli`).
  New `source.platform` holds the runtime (`web` | `server` | `app` | ...).
- `source.id` and `source.previous_id` are removed.
- Browser source now sets `source.url` and `source.referrer`.
- MCP source sets `source.tool` per emission. CLI source sets `source.command`.
- `Entity.nested` and `Entity.context` are now optional. Root `event.nested` and
  `event.context` remain required.
- Each source self-registers via TypeScript module augmentation of `SourceMap`
  in `@walkeros/core`.
- App-side coordination (`/workspaces/developer/app`) is a follow-up plan, not
  part of this release. Telemetry from v4 CLI/MCP will not validate against the
  existing app schema until that follow-up ships.
- `Mapping.Rule.skip` is renamed to `Mapping.Rule.silent`. Customer flow.json
  configs using `skip: true` in mapping rules must rename to `silent: true`.
  Hard cut: no legacy alias, the field is gone.
