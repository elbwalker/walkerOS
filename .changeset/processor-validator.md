---
'@walkeros/processor-validator': minor
---

Add event validation processor using AJV with JSON Schema

- Format validation: Pre-compiled WalkerOS.Event structure validation
- Contract validation: Entity/action keyed business rules with lazy compilation
- Supports wildcards, conditional rules, and schema caching via WeakMap
