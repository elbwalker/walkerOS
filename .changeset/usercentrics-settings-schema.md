---
'@walkeros/web-source-cmp-usercentrics': patch
---

The settings schema now lists `apiVersion` (`auto`, `v2` or `v3`) and
`v3EventName`, which the source already supported, so `walkeros validate` and
editors accept them.
