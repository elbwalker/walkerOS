{
  "workspaces": [
    "packages/*",
    "packages/config/*",
    "packages/clients/*",
    "packages/destinations/node/*",
    "packages/destinations/web/*"
  ],
  "scripts": {
    "build": "turbo run build --filter=!@elbwalker/docs",
    "build:docs": "turbo run build --filter=@elbwalker/docs",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@elbwalker/jest": "*",
    "@elbwalker/tsconfig": "*",
    "@elbwalker/tsup": "*",
    "@elbwalker/types": "*",
    "@elbwalker/utils": "*",
    "eslint-config-custom": "*",
    "turbo": "^1.10.12",
    "typescript": "^5.1.6"
  },
  "packageManager": "npm@9.6.7",
  "browserslist": [
    "> 0.2%",
    "not dead",
    "not ie < 11",
    "not op_mini all"
  ]
}
