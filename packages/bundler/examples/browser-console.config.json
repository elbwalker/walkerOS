{
  "packages": [
    {
      "name": "@walkeros/core",
      "version": "latest"
    }
  ],
  "customCode": "import { getId, getByPath, clone, trim } from '@walkeros/core';\n\n// Make walkerOS utilities available globally for console testing\nwindow.generateId = (length = 8) => getId(length);\nwindow.extractData = (obj, path) => getByPath(obj, path, null);\nwindow.deepClone = (obj) => clone(obj);\nwindow.cleanString = (str) => trim(str);\nwindow.walkerUtils = { getId, getByPath, clone, trim };\n\nconsole.log('✅ Tree-shaken bundle loaded! Only 4 walkerOS utilities included.');\nconsole.log('Try: generateId(12)');\nconsole.log('Try: extractData({user: {name: \"John\"}}, \"user.name\")');\nconsole.log('Try: deepClone({a: {b: 1}})');\nconsole.log('Try: cleanString(\"  hello world  \")');",
  "build": {
    "platform": "browser",
    "format": "iife",
    "target": "es2018",
    "minify": true,
    "sourcemap": false
  },
  "output": {
    "filename": "browser-console.js",
    "dir": "./dist"
  }
}
