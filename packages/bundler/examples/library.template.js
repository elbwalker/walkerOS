/**
 * {{NAME}} Library
 * Version: {{VERSION}}
 * Built: {{BUILD_DATE}}
 */

{
  {
    BUNDLE;
  }
}

const library = {
  name: '{{NAME}}',
  version: '{{VERSION}}',
  exports: typeof exports !== 'undefined' ? exports : {},
};

export default library;
