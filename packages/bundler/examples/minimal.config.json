{
  "packages": [
    {
      "name": "@walkeros/core",
      "version": "latest"
    }
  ],
  "content": "import { getId, trim } from '@walkeros/core';\n\n// Generate unique identifiers\nexport const generateId = (length = 8) => getId(length);\n\n// Clean and trim strings\nexport const cleanString = (str) => trim(str);\n\n// Only getId and trim from @walkeros/core are bundled - tree-shaken!",
  "build": {
    "platform": "browser",
    "format": "esm",
    "target": "es2018"
  },
  "output": {
    "filename": "minimal.js",
    "dir": "./dist"
  }
}
