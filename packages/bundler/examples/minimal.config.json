{
  "packages": [
    {
      "name": "lodash-es",
      "version": "4.17.21"
    }
  ],
  "customCode": "import { map, chunk } from 'lodash-es';\n\nexport const double = (n) => n * 2;\nexport const processArray = (arr) => chunk(map(arr, double), 3);\n\n// Only map and chunk from lodash are bundled - tree-shaken!",
  "build": {
    "platform": "browser",
    "format": "esm",
    "target": "es2018"
  },
  "output": {
    "filename": "minimal.js",
    "dir": "./dist"
  }
}
