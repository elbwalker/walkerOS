/**
 * {{NAME}} Library
 * Version: {{VERSION}}
 * Built: {{BUILD_DATE}}
 */

{{CONTENT}}

const library = {
  name: '{{NAME}}',
  version: '{{VERSION}}',
  exports: typeof exports !== 'undefined' ? exports : {},
};

export default library;
