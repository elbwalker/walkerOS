/**
 * The simulate output formatter lives in `@walkeros/core/node` so the CLI
 * `push` output and the MCP `flow_simulate` result print values the same way.
 */
export { toPrintable } from '@walkeros/core/node';
