{{CONTENT}}

export default (async function() {
  const config = {
    sources: {
    {{#sources}}
      {{name}}: {
        code: {{{code}}},
        config: {{{config}}},
        env: {{{env}}}
      },
    {{/sources}}
    },
    destinations: {
    {{#destinations}}
      {{name}}: {
        code: {{{code}}},
        config: {{{config}}},
        env: {{{env}}}
      },
    {{/destinations}}
    }
  };

  return await createCollector(config);
})();