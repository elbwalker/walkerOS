import { transformerValidate } from '../index';

describe('transformerValidate scaffold', () => {
  test('is exported as a function', () => {
    expect(typeof transformerValidate).toBe('function');
  });
});
