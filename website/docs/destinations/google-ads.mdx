---
title: Google Ads
description: How to send conversion events to Google Ads with walker.js
---

import PackageLink from '/src/components/docs/package-link';
import Link from '@docusaurus/Link';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import {
  DestinationContextProvider,
  DestinationInit,
  DestinationPush,
} from '@site/src/components/organisms/liveDestination';
import destinationGoogleAds from '@elbwalker/destination-web-google-ads';
import { mapping } from '@elbwalker/destination-web-google-ads/examples';
import { getEvent } from '@elbwalker/utils';

<DestinationContextProvider
  destination={destinationGoogleAds}
  fnName='gtag'
>

<PackageLink
  browser="@elbwalker/destination-web-google-ads"
  es5="@elbwalker/destination-web-google-ads"
  github="packages/destinations/web/google-ads"
  npm="@elbwalker/destination-web-google-ads"
/>

[Google Ads](https://ads.google.com/) is Google's comprehensive advertising
platform. The walker.js Google Ads destination enables seamless integration of
your web application with Google Ads.

## Installation

<Tabs>
  <TabItem value="ts" label="TypeScript" default>
Install the destination via npm

```bash
npm i @elbwalker/destination-web-google-ads
```

```ts
import { elb } from '@elbwalker/walker.js';
import destinationGoogleAds from '@elbwalker/destination-web-google-ads';

elb('walker destination', destinationGoogleAds, config);
```

  </TabItem>
  <TabItem value="script" label="Script">

Load the destination via dynamic import

```html
<script>
  // Upload the dist/index.mjs on your own server
  const destination = (
    await import(
      'https://cdn.jsdelivr.net/npm/@elbwalker/destination-web-google-ads/dist/index.mjs'
    )
  ).default;

  elb('walker destination', destination, config);
</script>
```

  </TabItem>
  <TabItem value="code" label="Code">
Copy the code from the&nbsp;<Link to="https://cdn.jsdelivr.net/npm/@elbwalker/destination-web-api@latest/dist/index.browser.js">index.browser.js</Link> file

```js
(function () {
  'use strict';
  var Destination = (() => {})(); // This is the copied code

  elb('walker destination', Destination.default, config);
})();
```

  </TabItem>
</Tabs>

## Configuration

Configure the Google Ads destination with a Conversion ID and Conversion label.

<DestinationInit
  custom={`{
    conversionId: 'AW-123456789', // Required
    currency: 'EUR',
    defaultValue: 1,
  }`}
/>

### Custom

| Property       | Type   | Description                                      |
| -------------- | ------ | ------------------------------------------------ |
| conversionId\* | string | Google Ads conversion ID, starting with 'AW-'' . |
| currency       | string | Default currency for conversions.                |
| defaultValue   | number | Default value used for conversions.              |

### CustomEvent

For the custom event mapping (`mapping.entity.action.custom`):

| Property | Type   | Description                                     |
| -------- | ------ | ----------------------------------------------- |
| label\*  | string | Google Ads Conversion label.                    |
| id       | string | Property key to use as transaction ID.          |
| value    | string | Name of the data property key to use for value. |

## Examples

### Order

<DestinationPush
  smallText={true}
  height={'256px'}
  event={getEvent('order complete')}
  mapping={mapping.conversion}
/>

</DestinationContextProvider>

:::info

If you need professional support with your walkerOS implementation, check out
our&nbsp;<Link to="/services">services</Link>.

:::
