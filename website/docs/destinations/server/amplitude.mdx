---
title: Amplitude
description: "Server-side event delivery to Amplitude for product analytics with per-event identity"
path: /docs/destinations/server/amplitude
sidebar_position: 6
package: '@walkeros/server-destination-amplitude'
---
import data from '@walkeros/server-destination-amplitude/walkerOS.json';
import Settings from '@site/src/components/snippets/_settings.mdx';

# Amplitude

<PackageLink env="server" github="packages/server/destinations/amplitude" npm="@walkeros/server-destination-amplitude" />

Server-side event delivery to [Amplitude](https://amplitude.com/) for product analytics, using the official [`@amplitude/analytics-node`](https://www.npmjs.com/package/@amplitude/analytics-node) SDK. Supports `track`, `identify`, `revenue`, `setGroup`, `groupIdentify`, and `flush` calls with per-event identity via EventOptions.

:::info Where This Fits
Amplitude is a **server destination** in the walkerOS flow:

<FlowMap
  sources={{ default: { highlight: false } }}
  collector={{ highlight: false }}
  destinations={{ default: { label: 'Destination', text: 'Amplitude', highlight: true } }}
/>

Sends events server-side to Amplitude's HTTP API, providing reliable delivery with identity resolution, revenue tracking, and group analytics without browser-side SDK overhead.
:::

## Installation

<CodeSnippet
  code={`npm install @walkeros/server-destination-amplitude`}
  language="bash"
/>

## Setup

<Tabs groupId="mode">
  <TabItem value="integrated" label="Integrated" default>

<CodeSnippet
  code={`import { startFlow } from '@walkeros/collector';
import { destinationAmplitude } from '@walkeros/server-destination-amplitude';

await startFlow({
  destinations: {
    amplitude: {
      code: destinationAmplitude,
      config: {
        settings: {
          apiKey: 'YOUR_AMPLITUDE_API_KEY',
        },
      },
    },
  },
});`}
  language="typescript"
/>

  </TabItem>
  <TabItem value="bundled" label="Bundled">

Add to your `flow.json` destinations:

<CodeSnippet
  code={`"destinations": {
  "amplitude": {
    "package": "@walkeros/server-destination-amplitude",
    "code": "destinationAmplitude",
    "config": {
      "settings": {
        "apiKey": "YOUR_AMPLITUDE_API_KEY"
      }
    }
  }
}`}
  language="json"
/>

[See Bundled Mode setup →](/docs/getting-started/modes/bundled) | [CLI reference →](/docs/apps/cli)

  </TabItem>
</Tabs>

<Settings
  schema={data.schemas.settings}
  type="destination"
  configHref="/docs/destinations#configuration"
/>
