---
title: AWS Firehose
description:
  Stream events to Amazon Kinesis Data Firehose for real-time analytics
path: /docs/destinations/server/aws
sidebar_position: 1
package: '@walkeros/server-destination-aws'
---

import PackageLink from '@site/src/components/docs/package-link';
import SettingsTable from '@site/src/components/molecules/SettingsTable';

# AWS Firehose

<PackageLink
  env="server"
  github="packages/server/destinations/aws"
  npm="@walkeros/server-destination-aws"
/>

## Installation

```bash
npm install @walkeros/server-destination-aws
```

## Setup

```typescript
import { elb } from '@walkeros/collector';
import { destinationFirehose } from '@walkeros/server-destination-aws';

elb('walker destination', destinationFirehose, {
  settings: {
    firehose: {
      streamName: 'your-firehose-stream-name',
      region: 'eu-central-1',
      config: {
        credentials: {
          accessKeyId: 'your-access-key-id',
          secretAccessKey: 'your-secret-access-key',
        },
      },
    },
  },
});
```

## Configuration reference

<SettingsTable
  properties={[
    {
      name: 'firehose',
      type: 'FirehoseConfig',
      description: 'AWS Firehose configuration settings',
      required: false,
      example: "{ streamName: 'walker-events', region: 'us-east-1' }",
    },
  ]}
/>

### Firehose Configuration

<SettingsTable
  properties={[
    {
      name: 'streamName',
      type: 'string',
      description: 'Name of the Kinesis Data Firehose delivery stream',
      required: true,
      example: "'walker-events'",
    },
    {
      name: 'client',
      type: 'FirehoseClient',
      description: 'Pre-configured AWS Firehose client instance',
      required: false,
      example: 'new FirehoseClient(config)',
    },
    {
      name: 'region',
      type: 'string',
      description: 'AWS region for the Firehose service',
      required: false,
      example: "'us-east-1'",
    },
    {
      name: 'config',
      type: 'FirehoseClientConfig',
      description: 'AWS SDK client configuration options',
      required: false,
      example: '{ credentials: awsCredentials }',
    },
  ]}
/>
