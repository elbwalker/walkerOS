---
title: GCP BigQuery
description:
  Stream events to Google BigQuery for data analytics and machine learning
path: /docs/destinations/server/gcp
sidebar_position: 2
tags: [server, gcp, bigquery, analytics, data-warehouse, streaming]
package: '@walkeros/server-destination-gcp'
---

import PackageLink from '@site/src/components/docs/package-link';
import SettingsTable from '@site/src/components/molecules/SettingsTable';

# GCP BigQuery

<PackageLink
  env="server"
  github="packages/server/destinations/gcp"
  npm="@walkeros/server-destination-gcp"
/>

## Configuration

<SettingsTable
  properties={[
    {
      name: 'client',
      type: 'BigQuery',
      description: 'Google Cloud BigQuery client instance',
      required: true,
      example: 'new BigQuery({ projectId, keyFilename })',
    },
    {
      name: 'projectId',
      type: 'string',
      description: 'Google Cloud Project ID',
      required: true,
      example: "'my-gcp-project'",
    },
    {
      name: 'datasetId',
      type: 'string',
      description: 'BigQuery dataset ID where events will be stored',
      required: true,
      example: "'walker_events'",
    },
    {
      name: 'tableId',
      type: 'string',
      description: 'BigQuery table ID for event storage',
      required: true,
      example: "'events'",
    },
    {
      name: 'location',
      type: 'string',
      description: 'Geographic location for the BigQuery dataset',
      required: false,
      example: "'US'",
    },
    {
      name: 'bigquery',
      type: 'BigQueryOptions',
      description: 'Additional BigQuery client configuration options',
      required: false,
      example: '{ keyFilename: "path/to/key.json" }',
    },
  ]}
/>
