---
title: Meta Conversions API
description: "Server-side Meta advertising events for enhanced privacy and tracking accuracy"
path: /docs/destinations/server/meta-capi
sidebar_position: 3
package: '@walkeros/server-destination-meta'
---
import data from '@walkeros/server-destination-meta/walkerOS.json';
import Configuration from '@site/src/components/snippets/_configuration.mdx';

import WhereThisFits from '@site/src/components/snippets/_where-this-fits.mdx';

# Meta Conversions API

<PackageLink env="server" github="packages/server/destinations/meta" npm="@walkeros/server-destination-meta" />

Server-side event delivery to Meta's Conversions API for enhanced tracking accuracy and privacy compliance.

<WhereThisFits
  pre={<>Meta CAPI is a <strong>server destination</strong> in the walkerOS flow:</>}
  flowMap={{
    destinations: { default: { label: 'Destination', text: 'Meta CAPI', highlight: true } },
  }}
  post={<>Sends events server-side to Meta's Conversions API, bypassing browser limitations for improved data quality and privacy-compliant tracking.</>}
/>

## Installation

<CodeSnippet
  code={`npm install @walkeros/server-destination-meta`}
  language="bash"
/>

<Tabs groupId="mode">
  <TabItem value="integrated" label="Integrated" default>

<CodeSnippet
  code={`import { startFlow } from '@walkeros/collector';
import { destinationMeta } from '@walkeros/server-destination-meta';

await startFlow({
  destinations: {
    meta: {
      code: destinationMeta,
      config: {
        settings: {
          accessToken: 'YOUR_ACCESS_TOKEN',
          pixelId: 'YOUR_PIXEL_ID',
        },
      },
    },
  },
});`}
  language="typescript"
/>

  </TabItem>
  <TabItem value="bundled" label="Bundled">

Add to your `flow.json` destinations:

<CodeSnippet
  code={`"destinations": {
  "meta": {
    "package": "@walkeros/server-destination-meta",
    "import": "destinationMeta",
    "config": {
      "settings": {
        "accessToken": "YOUR_ACCESS_TOKEN",
        "pixelId": "YOUR_PIXEL_ID"
      }
    }
  }
}`}
  language="json"
/>

[See bundled mode setup](/docs/getting-started/modes/bundled) | [CLI reference](/docs/apps/cli)

  </TabItem>
</Tabs>

<Configuration type="destination" data={data} />

## Client IP and user agent

The destination sends the client IP as `user_data.client_ip_address` and the user agent as `user_data.client_user_agent` by default. The `ip` and `userAgent` settings resolve against `{ ingest, event }` and default to `["ingest.ip", "event.user.ip"]` and `["ingest.userAgent", "event.user.userAgent"]`, so they fill in whenever the source provides them (see the `config.ingest` recipe on the [Express source](/docs/sources/server/express#ingest-metadata)). A value your mapping sets for `user_data.client_ip_address` or `user_data.client_user_agent` wins. A value that resolves to nothing is not sent. Set either setting to `false` to turn it off, or to another mapping value to read it elsewhere.
