---
title: Pinterest Conversions API
description: "Server-side event delivery to Pinterest Conversions API for enhanced conversion tracking"
path: /docs/destinations/server/pinterest
sidebar_position: 10
package: '@walkeros/server-destination-pinterest'
---
import data from '@walkeros/server-destination-pinterest/walkerOS.json';
import Configuration from '@site/src/components/snippets/_configuration.mdx';

import WhereThisFits from '@site/src/components/snippets/_where-this-fits.mdx';

# Pinterest Conversions API

<PackageLink env="server" github="packages/server/destinations/pinterest" npm="@walkeros/server-destination-pinterest" />

Server-side event delivery to Pinterest's Conversions API for enhanced conversion tracking, bypassing browser limitations for improved data quality and privacy compliance.

<WhereThisFits
  pre={<>Pinterest Conversions API is a <strong>server destination</strong> in the walkerOS flow:</>}
  flowMap={{
    destinations: { default: { label: 'Destination', text: 'Pinterest CAPI', highlight: true } },
  }}
  post={<>Sends events server-side to Pinterest's Conversions API via raw HTTP (`POST https://api.pinterest.com/v5/ad_accounts/{'{adAccountId}'}/events`), hashing 12 user_data fields with SHA-256 before transmission. Supports deduplication with the web Pinterest Tag via `event_id` + `event_name`.</>}
/>

## Installation

<CodeSnippet
  code={`npm install @walkeros/server-destination-pinterest`}
  language="bash"
/>

<Tabs groupId="mode">
  <TabItem value="integrated" label="Integrated" default>

<CodeSnippet
  code={`import { startFlow } from '@walkeros/collector';
import { destinationPinterest } from '@walkeros/server-destination-pinterest';

await startFlow({
  destinations: {
    pinterest: {
      code: destinationPinterest,
      config: {
        settings: {
          accessToken: 'YOUR_ACCESS_TOKEN',
          adAccountId: 'YOUR_AD_ACCOUNT_ID',
        },
      },
    },
  },
});`}
  language="typescript"
/>

  </TabItem>
  <TabItem value="bundled" label="Bundled">

Add to your `flow.json` destinations:

<CodeSnippet
  code={`"destinations": {
  "pinterest": {
    "package": "@walkeros/server-destination-pinterest",
    "import": "destinationPinterest",
    "config": {
      "settings": {
        "accessToken": "YOUR_ACCESS_TOKEN",
        "adAccountId": "YOUR_AD_ACCOUNT_ID"
      }
    }
  }
}`}
  language="json"
/>

[See bundled mode setup](/docs/getting-started/modes/bundled) | [CLI reference](/docs/apps/cli)

  </TabItem>
</Tabs>

<Configuration type="destination" data={data} />

## Client IP and user agent

The destination sends the client IP as `user_data.client_ip_address` and the user agent as `user_data.client_user_agent` by default. The `ip` and `userAgent` settings resolve against `{ ingest, event }` and default to `["ingest.ip", "event.user.ip"]` and `["ingest.userAgent", "event.user.userAgent"]`, so they fill in whenever the source provides them (see the `config.ingest` recipe on the [Express source](/docs/sources/server/express#ingest-metadata)). A value your mapping sets for `user_data.client_ip_address` or `user_data.client_user_agent` wins. A value that resolves to nothing is not sent. Set either setting to `false` to turn it off, or to another mapping value to read it elsewhere.
