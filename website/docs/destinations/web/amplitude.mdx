---
title: Amplitude
description: Product analytics, identity, revenue, and optional session replay, experiments, and guides & surveys
path: /docs/destinations/web/amplitude
sidebar_position: 6
package: '@walkeros/web-destination-amplitude'
---

import destinationAmplitude from '@walkeros/web-destination-amplitude';
import { examples, schemas } from '@walkeros/web-destination-amplitude/dev';

export const destination = { ...destinationAmplitude, examples };

# Amplitude

<PackageLink
  env="web"
  github="packages/web/destinations/amplitude"
  npm="@walkeros/web-destination-amplitude"
/>

[Amplitude](https://amplitude.com/) is a product analytics platform with
built-in identity, revenue, groups, and optional session replay, feature
experiments, and guides & surveys. This destination forwards walkerOS events
to Amplitude via the official
[`@amplitude/analytics-browser`](https://www.npmjs.com/package/@amplitude/analytics-browser)
SDK, translating events into `amplitude.track(...)`, `identify(...)`,
`revenue(...)`, `setGroup(...)`, and `setOptOut(...)` calls.

:::info Where This Fits
Amplitude is a **web destination** in the walkerOS flow:

<FlowMap
  sources={{ default: { highlight: false } }}
  collector={{ highlight: false }}
  destinations={{ default: { label: 'Destination', text: 'Amplitude', highlight: true } }}
/>

Loads the Amplitude Analytics SDK, forwards every walkerOS event, and lets
you layer on identity, revenue, groups, consent toggling, and optional
plugins.
:::

## Installation

<CodeSnippet
  code={`npm install @walkeros/web-destination-amplitude`}
  language="bash"
/>

## Setup

<Tabs groupId="mode">
  <TabItem value="integrated" label="Integrated" default>

<CodeSnippet
  code={`import { startFlow } from '@walkeros/collector';
import { destinationAmplitude } from '@walkeros/web-destination-amplitude';

await startFlow({
  destinations: {
    amplitude: {
      code: destinationAmplitude,
      config: {
        consent: { analytics: true },
        settings: {
          apiKey: 'YOUR_AMPLITUDE_API_KEY',
        },
      },
    },
  },
});`}
  language="typescript"
/>

  </TabItem>
  <TabItem value="bundled" label="Bundled">

Add to your `flow.json` destinations:

<CodeSnippet
  code={`"destinations": {
  "amplitude": {
    "package": "@walkeros/web-destination-amplitude",
    "config": {
      "consent": { "analytics": true },
      "settings": {
        "apiKey": "YOUR_AMPLITUDE_API_KEY"
      }
    }
  }
}`}
  language="json"
/>

[See Bundled Mode setup →](/docs/getting-started/modes/bundled) | [CLI reference →](/docs/apps/cli)

  </TabItem>
</Tabs>

## Configuration reference

All fields of Amplitude's `BrowserOptions` pass through to
`amplitude.init(apiKey, options)`. walkerOS-specific additions are:

<PropertyTable schema={schemas.settings} />

## Mapping examples

:::tip

The code examples are interactive. You can edit the code to generate your
individual event mapping.

:::

### Default event forwarding

Every walkerOS event becomes `amplitude.track(event.name, event_properties)`.
Without `include` config, `event_properties` is empty. Add `include: ['data']`
to the destination config to forward event data as properties.

<DestinationDemo
  destination={destination}
  settings={{ apiKey: 'YOUR_AMPLITUDE_API_KEY' }}
  event={examples.step.defaultEventForwarding.in}
  mapping={{ include: ['data'] }}
  generic={true}
/>

### User login identify

Set the user ID and enrich user properties with the full operation
vocabulary (`set`, `setOnce`, `add`, ...). Use `skip: true` to suppress
the default track call.

<DestinationDemo
  destination={destination}
  settings={{ apiKey: 'YOUR_AMPLITUDE_API_KEY' }}
  event={examples.step.userLoginIdentify.in}
  mapping={examples.step.userLoginIdentify.mapping}
  generic={true}
/>

### Multi-product order with revenue loop

Use `loop` inside `settings.revenue` to fire one `amplitude.revenue()`
call per nested product. The order-level `track()` still fires once.

<DestinationDemo
  destination={destination}
  settings={{ apiKey: 'YOUR_AMPLITUDE_API_KEY' }}
  event={examples.step.orderCompleteMultiProduct.in}
  mapping={examples.step.orderCompleteMultiProduct.mapping}
  generic={true}
/>

### Subscription revenue

Single-object revenue mapping for non-cart flows like subscription
renewals. Currency falls back to `"EUR"` when `data.currency` is missing.

<DestinationDemo
  destination={destination}
  settings={{ apiKey: 'YOUR_AMPLITUDE_API_KEY' }}
  event={examples.step.subscriptionRenewRevenue.in}
  mapping={examples.step.subscriptionRenewRevenue.mapping}
  generic={true}
/>

### Group assignment with properties

Assign a user to a group and enrich group properties in one rule.

<DestinationDemo
  destination={destination}
  settings={{ apiKey: 'YOUR_AMPLITUDE_API_KEY' }}
  event={examples.step.groupAssignmentWithProperties.in}
  mapping={examples.step.groupAssignmentWithProperties.mapping}
  generic={true}
/>

## Consent

Declare the required consent keys on `config.consent`. When a walker
consent event arrives, the destination's `on('consent')` handler toggles
`amplitude.setOptOut()` with strict semantics: all required keys must be
granted for opt-in, any missing grant opts out.

```typescript
config: {
  consent: { analytics: true },
  settings: { apiKey: 'YOUR_KEY' },
}
```

## Plugins

All three plugin packages are npm-bundled with the destination (~135 KB
for Session Replay plus Experiment and Engagement payloads). Enable them
via destination settings:

```typescript
settings: {
  apiKey: 'YOUR_KEY',
  sessionReplay: { sampleRate: 1 },
  experiment: { deploymentKey: 'DEPLOYMENT_KEY' },
  engagement: true,
}
```

## Related

- [Amplitude Browser SDK](https://amplitude.com/docs/sdks/analytics/browser/browser-sdk-2)
- [Amplitude Session Replay](https://amplitude.com/docs/session-replay)
- [Amplitude Experiment](https://amplitude.com/docs/experiment)
- [Amplitude Guides & Surveys](https://amplitude.com/docs/guides-and-surveys)
