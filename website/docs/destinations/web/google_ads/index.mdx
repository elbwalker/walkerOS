---
title: Google Ads
description: How to send conversion events to Google Ads with walker.js
---

import PackageLink from '@site/src/components/docs/package-link';
import Link from '@docusaurus/Link';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import {
  DestinationContextProvider,
  DestinationInit,
  DestinationPush,
} from '@site/src/components/organisms/liveDestination';
import { destinationAds } from '@walkerOS/web-destination-google_ads';
import { destinationAdsExamples } from '@walkerOS/web-destination-google_ads/examples';
import { getEvent } from '@walkerOS/utils';

<DestinationContextProvider
  destination={destinationAds}
  fnName='gtag'
>

<PackageLink
  browser="@walkerOS/web-destination-google_ads"
  es5="@walkerOS/web-destination-google_ads"
  github="packages/web/destinations/google_ads"
  npm="@walkerOS/web-destination-google_ads"
/>

[Google Ads](https://ads.google.com/) is Google's comprehensive advertising
platform. The walker.js Google Ads destination enables seamless integration of
your web application with Google Ads.

## Installation

<Tabs>
  <TabItem value="ts" label="TypeScript" default>
Install the destination via npm

```ts
import { elb } from '@walkerOS/web-collector';
import { destinationAds } from '@walkerOS/web-destination-google_ads';

elb('walker destination', destinationAds, config);
```

  </TabItem>
  <TabItem value="script" label="Script">

Load the destination via dynamic import

```html
<script>
  // Upload the dist/index.mjs on your own server
  const destination = (
    await import(
      'https://cdn.jsdelivr.net/npm/@walkerOS/web-destination-google_ads/dist/index.mjs'
    )
  ).destinationAds;

  elb('walker destination', destination, config);
</script>
```

  </TabItem>
  <TabItem value="code" label="Code">
Copy the code from the&nbsp;<Link to="https://cdn.jsdelivr.net/npm/@walkerOS/web-destination-google_ads@latest/dist/index.browser.js">index.browser.js</Link> file

```js
(function () {
  'use strict';
  var Destination = (() => {})(); // This is the copied code

  elb('walker destination', Destination.destinationAds, config);
})();
```

  </TabItem>
</Tabs>

## Configuration

Configure the Google Ads destination with a Conversion ID and Conversion label.

<DestinationInit
  custom={`{
    conversionId: 'AW-123456789', // Required
    currency: 'EUR',
    defaultValue: 1,
  }`}
/>

### Custom

| Property       | Type   | Description                                      |
| -------------- | ------ | ------------------------------------------------ |
| conversionId\* | string | Google Ads conversion ID, starting with 'AW-'' . |
| currency       | string | Default currency for conversions.                |
| defaultValue   | number | Default value used for conversions.              |

Use `name` to specify the conversion label and `data` for other parameters.

## Mapping

### conversion

<DestinationPush
  smallText={true}
  height={'256px'}
  event={getEvent('order complete')}
  mapping={destinationAdsExamples.mapping.conversion}
/>

</DestinationContextProvider>

:::info

If you need professional support with your walkerOS implementation, check out
our&nbsp;<Link to="/services">services</Link>.

:::
