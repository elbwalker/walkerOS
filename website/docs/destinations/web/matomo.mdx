---
title: Matomo
description: Self-hosted or cloud privacy-first web analytics (GA alternative)
path: /docs/destinations/web/matomo
sidebar_position: 4
package: '@walkeros/web-destination-matomo'
---

import data from '@walkeros/web-destination-matomo/walkerOS.json';
import Settings from '@site/src/components/snippets/_settings.mdx';

import Mapping from '@site/src/components/snippets/_mapping.mdx';

# Matomo

<PackageLink
  env="web"
  github="packages/web/destinations/matomo"
  npm="@walkeros/web-destination-matomo"
  status="beta"
/>

[Matomo](https://matomo.org/) is an open-source, privacy-focused web analytics
platform that works self-hosted or in Matomo Cloud. This destination forwards
walkerOS events to Matomo via the `_paq` command queue with support for page
views, custom events, ecommerce, goals, site search, content tracking, and
custom dimensions.

:::info Where This Fits
Matomo is a **web destination** in the walkerOS flow:

<FlowMap
  sources={{ default: { highlight: false } }}
  collector={{ highlight: false }}
  destinations={{ default: { label: 'Destination', text: 'Matomo', highlight: true } }}
/>

Loads the Matomo tracking script, configures the tracker URL and site ID, and
sends page views and custom events with GDPR-compliant analytics.
:::

## Installation

<CodeSnippet
  code={`npm install @walkeros/web-destination-matomo`}
  language="bash"
/>

## Setup

<Tabs groupId="mode">
  <TabItem value="integrated" label="Integrated" default>

<CodeSnippet
  code={`import { startFlow } from '@walkeros/collector';
import { destinationMatomo } from '@walkeros/web-destination-matomo';

await startFlow({
  destinations: {
    matomo: {
      code: destinationMatomo,
      config: {
        loadScript: true,
        settings: {
          siteId: '1', // Required
          url: 'https://analytics.example.com/', // Required
        },
      },
    },
  },
});`}
  language="typescript"
/>

  </TabItem>
  <TabItem value="bundled" label="Bundled">

Add to your `flow.json` destinations:

<CodeSnippet
  code={`"destinations": {
  "matomo": {
    "package": "@walkeros/web-destination-matomo",
    "config": {
      "loadScript": true,
      "settings": {
        "siteId": "1",
        "url": "https://analytics.example.com/"
      }
    }
  }
}`}
  language="json"
/>

[See Bundled Mode setup →](/docs/getting-started/modes/bundled) | [CLI reference →](/docs/apps/cli)

  </TabItem>
</Tabs>

<Settings
  schema={data.schemas.settings}
  type="destination"
  configHref="/docs/destinations#configuration"
/>

#### Init example

`In` is the destination config you pass to `startFlow`; `Out` is what the destination emits when it bootstraps.

<StepExample example={data.examples.step.init} />

<Mapping schema={data.schemas.mapping} />

## Examples

The examples below show input, mapping, and the resulting vendor calls.

### Page View

Default handling -- no mapping needed.

<StepExample example={data.examples.step.pageView} />

### Custom Event with Goal

Map a walkerOS event to a Matomo `trackEvent` call with a `trackGoal` alongside.

<StepExample example={data.examples.step.customEvent} />

### Product Detail View

Map a walkerOS product view to an
&nbsp;<Link to="https://developer.matomo.org/api-reference/tracking-javascript#ecommerce">ecommerceProductDetailView</Link>
call.

<StepExample example={data.examples.step.productDetailView} />

### Ecommerce Cart Update

Map a walkerOS `cart view` event to an
&nbsp;<Link to="https://developer.matomo.org/api-reference/tracking-javascript#ecommerce">trackEcommerceCartUpdate</Link>
call.

<StepExample example={data.examples.step.ecommerceCartUpdate} />

### Ecommerce Order

Map a walkerOS `order complete` event to an
&nbsp;<Link to="https://developer.matomo.org/api-reference/tracking-javascript#ecommerce">trackEcommerceOrder</Link>
call.

<StepExample example={data.examples.step.ecommerceOrder} />

### Site Search

Track internal site search via `trackSiteSearch`.

<StepExample example={data.examples.step.siteSearch} />
