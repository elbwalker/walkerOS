---
title: Matomo
description: Self-hosted or cloud privacy-first web analytics (GA alternative)
path: /docs/destinations/web/matomo
sidebar_position: 4
package: '@walkeros/web-destination-matomo'
---

import data from '@walkeros/web-destination-matomo/walkerOS.json';
import Configuration from '@site/src/components/snippets/_configuration.mdx';

import WhereThisFits from '@site/src/components/snippets/_where-this-fits.mdx';

# Matomo

<PackageLink
  env="web"
  github="packages/web/destinations/matomo"
  npm="@walkeros/web-destination-matomo"
  status="beta"
/>

[Matomo](https://matomo.org/) is an open-source, privacy-focused web analytics
platform that works self-hosted or in Matomo Cloud. This destination forwards
walkerOS events to Matomo via the `_paq` command queue with support for page
views, custom events, ecommerce, goals, site search, content tracking, and
custom dimensions.

<WhereThisFits
  pre={<>Matomo is a <strong>web destination</strong> in the walkerOS flow:</>}
  flowMap={{
    destinations: { default: { label: 'Destination', text: 'Matomo', highlight: true } },
  }}
/>

## Installation

<CodeSnippet
  code={`npm install @walkeros/web-destination-matomo`}
  language="bash"
/>

<Tabs groupId="mode">
  <TabItem value="integrated" label="Integrated" default>

<CodeSnippet
  code={`import { startFlow } from '@walkeros/collector';
import { destinationMatomo } from '@walkeros/web-destination-matomo';

await startFlow({
  destinations: {
    matomo: {
      code: destinationMatomo,
      config: {
        loadScript: true,
        settings: {
          siteId: '1',
          url: 'https://analytics.example.com/',
        },
      },
    },
  },
});`}
  language="typescript"
/>

  </TabItem>
  <TabItem value="bundled" label="Bundled">

Add to your `flow.json` destinations:

<CodeSnippet
  code={`"destinations": {
  "matomo": {
    "package": "@walkeros/web-destination-matomo",
    "config": {
      "loadScript": true,
      "settings": {
        "siteId": "1",
        "url": "https://analytics.example.com/"
      }
    }
  }
}`}
  language="json"
/>

[See bundled mode setup](/docs/getting-started/modes/bundled) | [CLI reference](/docs/apps/cli)

  </TabItem>
</Tabs>

<Configuration type="destination" data={data} />

## Unmapped events

An event without `rule.name` that is not a page view and sets no tracking flag
(`siteSearch`, `contentImpression` or `contentInteraction`) sends only its goal
when the rule has `settings.goalId`, so a goal-only rule needs no name. Without
a goal, such an event is skipped: the destination logs a warning once per event
name, then repeats at debug level.

A `goalId` must be a Matomo goal ID, a positive integer like `1` or `"1"`.
Matomo drops any other conversion, so the destination skips that goal with a
warning and still sends the event's own hit.

## Custom dimensions

Custom dimensions are keyed by bare dimension id, like
`{ "1": "data.category" }`. Each value is a mapping value, resolved for every
event.

- `settings.customDimensions` applies to every hit.
- A rule's `settings.customDimensions` applies to that rule's hits and wins
  per key. A rule dimension that resolves to nothing removes that dimension
  for the hit, even when the destination sets it.

The dimensions are set with `setCustomDimension` right before the event's
hits, a goal included. After them, rule-only dimensions are deleted with
`deleteCustomDimension` and the destination values are set again, so a rule
value never leaks into later requests such as outlinks or the heart beat. With
destination dimension `1` set to `globals.pagegroup` and a page view rule
dimension `2` set to `data.id`, a page view pushes:

<CodeSnippet
  code={`_paq.push(['setCustomDimension', 1, 'docs']);
_paq.push(['setCustomDimension', 2, '/docs/']);
_paq.push(['trackPageView', 'walkerOS documentation']);
_paq.push(['deleteCustomDimension', 2]);
_paq.push(['setCustomDimension', 1, 'docs']);`}
  language="javascript"
/>
