---
title: Meta Pixel
description: Facebook and Instagram advertising pixel integration
path: /docs/destinations/web/meta-pixel
sidebar_position: 2
package: '@walkeros/web-destination-meta'
---

import PackageLink from '@site/src/components/docs/package-link';
import SettingsTable from '@site/src/components/molecules/SettingsTable';
import { getEvent } from '@walkeros/core';
import {
  DestinationInit,
  DestinationPush,
} from '@site/src/components/organisms/liveDestination';
import destinationMetaPixel from '@walkeros/web-destination-meta';
import { examples } from '@walkeros/web-destination-meta';

export const documentationMetaPixel = {
  ...destinationMetaPixel,
  env: {
    window: {
      fbq: () => {},
    },
  },
};

# Meta Pixel

<PackageLink
  env="web"
  github="packages/web/destinations/meta"
  npm="@walkeros/web-destination-meta"
/>

[Meta Pixel (formerly Facebook Pixel)](https://developers.facebook.com/docs/meta-pixel/)
tracks visitor activity and conversions for Facebook and Instagram advertising
campaigns. The destination automatically sends events to Meta Pixel via `fbq`
function. All you need to do is install the destination, and optionally map
events to Meta's format. walkerOS takes care of loading the script and the
function calls.

## Installation

```bash
npm install @walkeros/web-destination-meta
```

## Setup

```typescript
import { startFlow } from '@walkeros/collector';
import { destinationMeta } from '@walkeros/web-destination-meta';

const { collector, elb } = await startFlow({
  destinations: {
    meta: {
      code: destinationMeta,
      config: {
        settings: {
          pixelId: '1234567890', // Your Meta Pixel ID
        },
        loadScript: true, // Load Meta Pixel script automatically
      },
    },
  },
});
```

## Configuration reference

<SettingsTable
  properties={[
    {
      name: 'pixelId',
      type: 'string',
      description: 'Your Meta Pixel ID from Facebook Business Manager',
      required: true,
      example: "'1234567890'",
    },
    {
      name: 'loadScript',
      type: 'boolean',
      description:
        'Whether to automatically load the Meta Pixel script (fbevents.js)',
      required: false,
      example: 'true',
    },
  ]}
/>

#### Example

<DestinationInit
  destination={destinationMetaPixel}
  custom={{
    pixelId: '1234567890',
  }}
/>

## Mapping examples

:::tip

The code examples are interactive. You can edit the code to generate your
individual event mapping.

:::

Learn more aboot
[Meta Pixel's Standard-Events.](https://developers.facebook.com/docs/meta-pixel/reference#standard-events)

### ViewContent

<DestinationPush
  destination={documentationMetaPixel}
  settings={{
    pixelId: '1234567890',
  }}
  smallText={true}
  height={'512px'}
  event={getEvent('product view')}
  mapping={examples.mapping.ViewContent}
  generic={true}
/>

### AddToCart

<DestinationPush
  destination={documentationMetaPixel}
  settings={{
    pixelId: '1234567890',
  }}
  smallText={true}
  height={'512px'}
  event={getEvent('product add')}
  mapping={examples.mapping.AddToCart}
  generic={true}
/>

### InitiateCheckout

<DestinationPush
  destination={documentationMetaPixel}
  settings={{
    pixelId: '1234567890',
  }}
  smallText={true}
  height={'512px'}
  event={getEvent('cart view')}
  mapping={examples.mapping.InitiateCheckout}
  generic={true}
/>

### Purchase

<DestinationPush
  destination={documentationMetaPixel}
  settings={{
    pixelId: '1234567890',
  }}
  smallText={true}
  height={'512px'}
  event={getEvent('order complete')}
  mapping={examples.mapping.Purchase}
  generic={true}
/>
