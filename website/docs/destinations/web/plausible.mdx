---
title: Plausible Analytics
description: Simple, privacy-friendly and lightweight analytics
path: /docs/destinations/web/plausible
sidebar_position: 4
package: '@walkeros/web-destination-plausible'
---

import { getEvent } from '@walkeros/core';
import destinationPlausible from '@walkeros/web-destination-plausible';
import { examples, schemas } from '@walkeros/web-destination-plausible/dev';

export const destination = { ...destinationPlausible, examples };

# Plausible Analytics

<PackageLink
  env="web"
  github="packages/web/destinations/plausible"
  npm="@walkeros/web-destination-plausible"
/>

[Plausible Analytics](https://plausible.io/) is a simple, and privacy-friendly
Google Analytics Alternative. The destination automatically sends events to
Plausible via the `plausible` function. All you need to do is install the
destination, and optionally map events to Plausible's format. walkerOS takes
care of loading the script and the function calls.

:::info Where This Fits
Plausible is a **web destination** in the walkerOS flow:

<FlowMap
  sources={{ default: { highlight: false } }}
  collector={{ highlight: false }}
  destinations={{ default: { label: 'Destination', text: 'Plausible', highlight: true } }}
/>

Loads Plausible script, initializes the plausible function, and tracks page views and custom events with privacy-first analytics.
:::

## Installation

<CodeSnippet
  code={`npm install @walkeros/web-destination-plausible`}
  language="bash"
/>

## Setup

<Tabs groupId="mode">
  <TabItem value="integrated" label="Integrated" default>

<CodeSnippet
  code={`import { startFlow } from '@walkeros/collector';
import { destinationPlausible } from '@walkeros/web-destination-plausible';

await startFlow({
  destinations: {
    plausible: {
      code: destinationPlausible,
      config: {
        settings: {
          domain: 'walkeros.io', // Optional, domain of your site as registered
        },
      },
    },
  },
});`}
  language="typescript"
/>

  </TabItem>
  <TabItem value="bundled" label="Bundled">

Add to your `flow.json` destinations:

<CodeSnippet
  code={`"destinations": {
  "plausible": {
    "package": "@walkeros/web-destination-plausible",
    "config": {
      "settings": {
        "domain": "walkeros.io"
      }
    }
  }
}`}
  language="json"
/>

[See Bundled Mode setup →](/docs/getting-started/modes/bundled) | [CLI reference →](/docs/apps/cli)

  </TabItem>
</Tabs>

## Configuration reference

<PropertyTable schema={schemas.settings} />

<DestinationInitDemo
  destination={destination}
  settings={{
    domain: 'walkeros.io',
  }}
/>

## Mapping examples

:::tip

The code examples are interactive. You can edit the code to generate your
individual event mapping.

:::

Send&nbsp;<Link to="https://plausible.io/docs/custom-event-goals#trigger-custom-events-manually-with-a-javascript-function">custom
events</Link> manually using the JavaScript function

### CustomEvent

<DestinationDemo
  destination={destination}
  settings={{
    domain: 'walkeros.io',
  }}
  event={getEvent()}
  mapping={examples.mapping.customEvent}
  generic={true}
/>

### Purchase

Checkout&nbsp;<Link to="https://plausible.io/docs/ecommerce-revenue-tracking">Plausible's
E-commerce revenue tracking</Link> guide to learn more about the setup within
Plausible.

<DestinationDemo
  destination={destination}
  settings={{
    domain: 'walkeros.io',
  }}
  event={getEvent('order complete')}
  mapping={examples.mapping.purchase}
  generic={true}
/>
