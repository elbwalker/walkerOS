---
title: Installation
sidebar_position: 2
description: How to install walkerOS utils
---

import Link from '@docusaurus/Link';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import PackageLink from '/src/components/docs/package-link';

### NPM

```bash
$ npm install @elbwalker/utils
```

### CDN

There are multiple build files available. Bundled with all functions, grouped
only for the core or web, and separated into individual files. The `es5` files
can be used in older browsers as well as with Google Tag Manager.

<Tabs>
  <TabItem value="script" label="Scripts" default>
    ```html
    <script src="https://cdn.jsdelivr.net/npm/@elbwalker/utils@latest/dist/web.js"></script>
    ```
  </TabItem>
  <TabItem value="module" label="Module">
    Load the `.mjs` module-file directly from the CDN or your own server.
    ```html
    <script type="module">
      const Utils = ( await import(
      'https://cdn.jsdelivr.net/npm/@elbwalker/utils@latest/dist/web.mjs' ) );
      console.log('id', Utils.getId())
    </script>
    ```
  </TabItem>
</Tabs>

:::warning

Be aware of the `latest` version in the URL, better use a specific version like
3.0.0

:::

:::info

If you need professional support with your walkerOS implementation, check out
our&nbsp;<Link to="/services">services</Link>.

:::
