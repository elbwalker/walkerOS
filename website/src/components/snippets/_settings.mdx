Settings configure this package and go under `config.settings` in the flow. For fields shared across all {props.type}s (consent, mapping, data, env, …), see <a href={props.configHref}>{props.type} configuration</a>.

<PropertyTable schema={props.schema} />
