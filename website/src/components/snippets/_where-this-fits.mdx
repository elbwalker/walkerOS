{/*
  Where-this-fits snippet.

  Props:
    pre:     JSX node (required) — sentence shown above the FlowMap.
             Pass JSX so inline formatting (e.g. <strong>) renders correctly.
             Markdown inside interpolated strings is NOT parsed by MDX, so
             use JSX fragments instead of markdown syntax:
               pre={<>Meta Pixel is a <strong>web destination</strong>:</>}
    post:    JSX node (optional) — paragraph(s) shown below the FlowMap.
    flowMap: object (optional) — FlowMap props that override the defaults.
             Defaults dim every slot (highlight: false). Pass the slot you
             want highlighted, e.g.
               flowMap={{ destinations: { default: { label: 'Destination', text: 'Meta Pixel', highlight: true } } }}
             Spread order means caller props replace defaults per-slot (no
             deep merge).
*/}

:::info Where this fits

{props.pre}

<FlowMap
  sources={{ default: { highlight: false } }}
  collector={{ highlight: false }}
  destinations={{ default: { highlight: false } }}
  {...(props.flowMap || {})}
/>

{props.post}

:::
