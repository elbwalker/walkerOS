{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "inputs": [
        "$TURBO_EXTENDS$",
        "scripts/**",
        "docs/**",
        "docusaurus.config.ts",
        "$TURBO_ROOT$/apps/scripts/validate-llms.ts"
      ]
    }
  }
}
